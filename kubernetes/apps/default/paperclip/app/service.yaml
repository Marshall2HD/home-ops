---
apiVersion: v1
kind: Service
metadata:
  name: paperclip
  labels:
    app.kubernetes.io/name: paperclip
    app.kubernetes.io/component: external-app
spec:
  type: ClusterIP
  selector: {}
  ports:
    - name: http
      port: 3100
      targetPort: http
      protocol: TCP
