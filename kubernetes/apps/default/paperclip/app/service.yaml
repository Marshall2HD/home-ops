---
apiVersion: v1
kind: Service
metadata:
  name: paperclip
  labels:
    app.kubernetes.io/name: paperclip
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3100
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: paperclip
