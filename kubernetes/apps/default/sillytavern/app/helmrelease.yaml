---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sillytavern
spec:
  chartRef:
    kind: OCIRepository
    name: sillytavern
  interval: 1h
  values:
    controllers:
      sillytavern:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/sillytavern/sillytavern
              tag: staging@sha256:5c1a53539ad4f5d48627a1c4c4b52e0b0bad08d0f33611716c1b560f51ba039d
            env:
              TZ: America/New_York
              SILLYTAVERN_LISTEN: "true"
              SILLYTAVERN_WHITELIST: '["::/0", "0.0.0.0/0"]'
              SILLYTAVERN_PORT: &port 8000
              SILLYTAVERN_ENABLESERVERPLUGINS: "true"
              SILLYTAVERN_EXTENSIONS_MODELS_CLASSIFICATION: "SamLowe/roberta-base-go_emotions-onnx"
              SILLYTAVERN_EXTENSIONS_MODELS_EMBEDDING: "mixedbread-ai/mxbai-embed-large-v1"
              CLAUDE_OAUTH_PROXY_BIND: "0.0.0.0"
              CLAUDE_OAUTH_PROXY_PORT: &proxyPort 8787
              CLAUDE_OAUTH_PROXY_ALLOW_NON_LOCAL: "true"
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  periodSeconds: 1
                  failureThreshold: 600
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 8Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
          proxy:
            port: *proxyPort
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.hades.casa"
          - "tavern.hades.casa"
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
      proxy:
        hostnames:
          - "tavern-proxy.hades.casa"
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: proxy
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /home/node/app/config
            subPath: config
          - path: /home/node/app/data
            subPath: data
          - path: /home/node/app/plugins
            subPath: plugins
          - path: /home/node/app/public/scripts/extensions/third-party
            subPath: extensions
          - path: /home/node/.claude
            subPath: claude
      tmp:
        type: emptyDir
        medium: Memory
        sizeLimit: 64Mi
        globalMounts:
          - path: /tmp
            subPath: tmp
          - path: /home/node/app/backups
            subPath: backups
